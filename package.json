{
  "name": "posterai-platform-codeguide",
  "version": "1.0.0",
  "description": "AI-powered poster creation platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "lint": "cd backend && eslint .",
    "precommit": "lint-staged"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.0.0",
    "husky": "^8.0.0",
    "lint-staged": "^13.0.0",
    "prettier": "^2.0.0"
  },
  "lint-staged": {
    "backend/**/*.js": "eslint --config backend/.eslintrc.json --resolve-plugins-relative-to backend"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run precommit"
    }
  }
}
